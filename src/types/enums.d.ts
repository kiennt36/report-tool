export enum EMAIL_STATUS {
	NOT_REGISTERED = 0,
	REGISTERED = 1,
	VERIFIED = 2,
	ERROR = 4,
	NOT_EXIST = 5,
}

export enum PROXY_STATUS {
	ACTIVE = 0,
	INACTIVE = 1,
	ERROR = 2,
}

export enum PROXY_VERSION {
	V4 = 4,
	V6 = 6,
}
